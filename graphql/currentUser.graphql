query CurrentUser {
  currentUser {
    id
    names
    lastNames
    email
    tasks {
      id
      title
      description
      cratedAt
      status
      finishDate
      authorId
    }
  }
}
