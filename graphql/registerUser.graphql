mutation RegisterUser($create: CreateUserInput!) {
  registerUser(create: $create) {
    id
    names
    lastNames
    email
    tasks {
      id
      title
      description
      cratedAt
      status
      finishDate
      authorId
    }
  }
}
